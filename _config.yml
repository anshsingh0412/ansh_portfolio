 headline:
    "I build fast, delightful web apps with React, Node, and clean UI.",
  about:
    "Passionate developer focused on performance, DX, and pixel-perfect design. I love shipping useful products and documenting the journey.",
  avatar:
    "https://images.unsplash.com/photo-1527980965255-d3b416303d12?w=400&auto=format&fit=crop&crop=faces",
  resumePDF: "/assets/Ansh-Resume.pdf", // replace with your file path
  resumeDOCX: "/assets/Ansh-Resume.docx", // replace with your file path
  portfolioPDF: "/assets/Ansh-Portfolio.pdf", // optional
  socials: [
    { label: "GitHub", icon: Github, href: "https://github.com/yourhandle" },
    { label: "LinkedIn", icon: Linkedin, href: "https://linkedin.com/in/yourhandle" },
    { label: "Website", icon: Globe, href: "https://yourdomain.com" },
    { label: "Email", icon: Mail, href: "mailto:you@example.com" },
  ],
  skills: [
    { group: "Frontend", list: ["React", "Next.js", "TypeScript", "Tailwind", "Framer Motion"] },
    { group: "Backend", list: ["Node.js", "Express", "MongoDB", "PostgreSQL", "REST/GraphQL"] },
    { group: "Tools", list: ["Git", "Figma", "Vite", "Docker", "AWS"] },
  ],
  projects: [
    {
      title: "SwiftCart — Headless eCommerce",
      blurb:
        "A blazing‑fast storefront with carts, payments, and admin dashboard.",
      tags: ["React", "Node", "Stripe", "Postgres"],
      links: {
        demo: "https://your-demo-link.com",
        source: "https://github.com/yourhandle/swiftcart",
      },
      image:
        "https://images.unsplash.com/photo-1498050108023-c5249f4df085?w=1200&auto=format&fit=crop",
    },
    {
      title: "PixForge — AI Image Studio",
      blurb:
        "Generate, edit, and manage visuals with clean workflows and presets.",
      tags: ["Next.js", "TypeScript", "AI", "Design"],
      links: {
        demo: "https://your-demo-link.com",
        source: "https://github.com/yourhandle/pixforge",
      },
      image:
        "https://images.unsplash.com/photo-1498050108023-c5249f4df085?w=1200&auto=format&fit=crop",
    },
    {
      title: "TaskPulse — Team Productivity",
      blurb:
        "Kanban, OKRs, and real‑time updates. Built for focus and speed.",
      tags: ["Vite", "WebSockets", "React Query"],
      links: {
        demo: "https://your-demo-link.com",
        source: "https://github.com/yourhandle/taskpulse",
      },
      image:
        "https://images.unsplash.com/photo-1521737604893-d14cc237f11d?w=1200&auto=format&fit=crop",
    },
  ],
  gallery: {
    tabs: [
      {
        key: "designs",
        label: "Designs",
        icon: Camera,
        items: [
          "https://images.unsplash.com/photo-1525097487452-6278ff080c31?w=1200&auto=format&fit=crop",
          "https://images.unsplash.com/photo-1545239351-1141bd82e8a6?w=1200&auto=format&fit=crop",
          "https://images.unsplash.com/photo-1545239351-1141bd82e8a6?w=1200&auto=format&fit=crop",
        ],
      },
      {
        key: "videos",
        label: "Videos",
        icon: Film,
        items: [
          "https://images.unsplash.com/photo-1500530855697-b586d89ba3ee?w=1200&auto=format&fit=crop",
          "https://images.unsplash.com/photo-1495562569060-2eec283d3391?w=1200&auto=format&fit=crop",
        ],
      },
      {
        key: "code",
        label: "Code Shots",
        icon: Code2,
        items: [
          "https://images.unsplash.com/photo-1518770660439-4636190af475?w=1200&auto=format&fit=crop",
          "https://images.unsplash.com/photo-1515879218367-8466d910aaa4?w=1200&auto=format&fit=crop",
        ],
      },
    ],
  },
};

// ================
// UTILITIES
// ================
const Section = ({ id, title, icon: Icon, children }) => (
  <section id={id} className="scroll-mt-24 py-12">
    <div className="max-w-6xl mx-auto px-4">
      <div className="flex items-center gap-2 mb-6">
        {Icon ? <Icon className="h-5 w-5" /> : null}
        <h2 className="text-2xl md:text-3xl font-semibold">{title}</h2>
      </div>
      {children}
    </div>
  </section>
);

const useTheme = () => {
  const [dark, setDark] = useState(true);
  useEffect(() => {
    document.documentElement.classList.toggle("dark", dark);
  }, [dark]);
  return { dark, setDark };
};

const SmoothLink = ({ to, children }) => (
  <button
    onClick={() => {
      const el = document.getElementById(to);
      el?.scrollIntoView({ behavior: "smooth", block: "start" });
    }}
    className="px-3 py-2 text-sm rounded-xl hover:bg-muted"
  >
    {children}
  </button>
);

// ================
// MAIN COMPONENT
// ================
export default function Portfolio() {
  const { dark, setDark } = useTheme();

  const variants = {
    hidden: { opacity: 0, y: 12 },
    show: { opacity: 1, y: 0 },
  };

  return (
    <div className="min-h-screen bg-background text-foreground selection:bg-primary selection:text-primary-foreground">
      {/* NAVBAR */}
      <header className="sticky top-0 z-50 backdrop-blur border-b bg-background/70">
        <div className="max-w-6xl mx-auto px-4 h-16 flex items-center justify-between">
          <div className="flex items-center gap-3">
            <img src={PROFILE.avatar} alt="avatar" className="h-9 w-9 rounded-2xl object-cover" />
            <div>
              <div className="font-semibold leading-tight">{PROFILE.name}</div>
              <div className="text-xs text-muted-foreground">{PROFILE.role}</div>
            </div>
          </div>

          <nav className="hidden md:flex items-center gap-1">
            <SmoothLink to="about">About</SmoothLink>
            <SmoothLink to="skills">Skills</SmoothLink>
            <SmoothLink to="projects">Projects</SmoothLink>
            <SmoothLink to="gallery">Gallery</SmoothLink>
            <SmoothLink to="resume">Resume</SmoothLink>
            <SmoothLink to="contact">Contact</SmoothLink>
          </nav>

          <div className="flex items-center gap-3">
            <div className="flex items-center gap-2 text-sm">
              <Sun className="h-4 w-4" />
              <Switch checked={dark} onCheckedChange={setDark} />
              <Moon className="h-4 w-4" />
            </div>
            <a href={PROFILE.resumePDF} target="_blank" rel="noreferrer">
              <Button className="rounded-2xl" size="sm">
                <Download className="h-4 w-4 mr-1" /> Resume
              </Button>
            </a>
          </div>
        </div>
      </header>

      {/* HERO */}
      <section className="bg-gradient-to-b from-muted/40 to-transparent">
        <div className="max-w-6xl mx-auto px-4 py-12 md:py-16 grid md:grid-cols-3 gap-6 items-center">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.5 }}
            className="md:col-span-2"
          >
            <Badge className="rounded-2xl">Open to Opportunities</Badge>
            <h1 className="text-3xl md:text-5xl font-bold leading-tight mt-4">
              {PROFILE.headline}
            </h1>
            <p className="text-muted-foreground mt-4 max-w-2xl">
              {PROFILE.about}
            </p>
            <div className="flex flex-wrap items-center gap-3 mt-6">
              {PROFILE.socials.map((s, i) => (
                <a
                  key={i}
                  href={s.href}
                  target="_blank"
                  className="inline-flex items-center gap-2 rounded-2xl border px-3 py-2 text-sm hover:bg-muted"
                  rel="noreferrer"
                >
                  {s.icon ? <s.icon className="h-4 w-4" /> : <LinkIcon className="h-4 w-4" />}
                  {s.label}
                </a>
              ))}
            </div>
          </motion.div>

          <motion.div
            initial={{ opacity: 0, scale: 0.95 }}
            animate={{ opacity: 1, scale: 1 }}
            transition={{ duration: 0.5, delay: 0.1 }}
          >
            <Card className="rounded-3xl overflow-hidden">
              <CardContent className="p-0">
                <img
                  src={PROFILE.avatar}
                  alt="profile"
                  className="w-full aspect-[4/5] object-cover"
                />
              </CardContent>
              <CardFooter className="flex items-center justify-between p-4">
                <div className="text-sm text-muted-foreground flex items-center gap-2">
                  <MapPin className="h-4 w-4" /> {PROFILE.location}
                </div>
                <div className="flex gap-2">
                  <a href={`mailto:${PROFILE.email}`}><Button size="icon" variant="outline" className="rounded-xl"><Mail className="h-4 w-4" /></Button></a>
                  <a href={`tel:${PROFILE.phone}`}><Button size="icon" variant="outline" className="rounded-xl"><Phone className="h-4 w-4" /></Button></a>
                </div>
              </CardFooter>
            </Card>
          </motion.div>
        </div>
      </section>

      {/* ABOUT */}
      <Section id="about" title="About" icon={User}>
        <div className="grid md:grid-cols-3 gap-6">
          <Card className="rounded-3xl md:col-span-2">
            <CardHeader>
              <h3 className="font-semibold">Who am I?</h3>
            </CardHeader>
            <CardContent className="text-muted-foreground">
              <p>
                {PROFILE.about}
              </p>
              <p className="mt-3">
                I care about accessibility, maintainability, and developer experience. I enjoy working across the stack and collaborating with designers & PMs.
              </p>
            </CardContent>
          </Card>
          <Card className="rounded-3xl">
            <CardHeader>
              <h3 className="font-semibold">Quick Facts</h3>
            </CardHeader>
            <CardContent className="text-sm text-muted-foreground grid gap-2">
              <div className="flex items-center gap-2"><MapPin className="h-4 w-4" />{PROFILE.location}</div>
              <div className="flex items-center gap-2"><Mail className="h-4 w-4" />{PROFILE.email}</div>
              <div className="flex items-center gap-2"><Phone className="h-4 w-4" />{PROFILE.phone}</div>
            </CardContent>
          </Card>
        </div>
      </Section>

      {/* SKILLS */}
      <Section id="skills" title="Skills" icon={FolderGit2}>
        <div className="grid md:grid-cols-3 gap-6">
          {PROFILE.skills.map((s, i) => (
            <Card key={i} className="rounded-3xl">
              <CardHeader>
                <h3 className="font-semibold flex items-center gap-2">{s.group}</h3>
              </CardHeader>
              <CardContent className="flex flex-wrap gap-2">
                {s.list.map((item, j) => (
                  <Badge key={j} variant="secondary" className="rounded-xl">{item}</Badge>
                ))}
              </CardContent>
            </Card>
          ))}
        </div>
      </Section>

      {/* PROJECTS */}
      <Section id="projects" title="Projects" icon={FolderGit2}>
        <div className="grid md:grid-cols-3 gap-6">
          {PROFILE.projects.map((p, i) => (
            <motion.div key={i} initial="hidden" whileInView="show" viewport={{ once: true }} variants={{ hidden: { opacity: 0, y: 16 }, show: { opacity: 1, y: 0 } }}>
              <Card className="rounded-3xl overflow-hidden">
                <CardContent className="p-0">
                  <img src={p.image} alt={p.title} className="w-full h-44 object-cover" />
                </CardContent>
                <CardHeader>
                  <div className="text-lg font-semibold">{p.title}</div>
                  <p className="text-sm text-muted-foreground">{p.blurb}</p>
                </CardHeader>
                <CardContent>
                  <div className="flex flex-wrap gap-2 mb-4">
                    {p.tags.map((t, j) => (
                      <Badge key={j} variant="outline" className="rounded-xl">{t}</Badge>
                    ))}
                  </div>
                  <div className="flex gap-3">
                    <a href={p.links.demo} target="_blank" rel="noreferrer">
                      <Button className="rounded-xl" variant="secondary" size="sm">
                        <ExternalLink className="h-4 w-4 mr-1" /> Live Demo
                      </Button>
                    </a>
                    <a href={p.links.source} target="_blank" rel="noreferrer">
                      <Button className="rounded-xl" variant="outline" size="sm">
                        <Github className="h-4 w-4 mr-1" /> Source
                      </Button>
                    </a>
                  </div>
                </CardContent>
              </Card>
            </motion.div>
          ))}
        </div>
      </Section>

      {/* GALLERY / CREATIVE */}
      <Section id="gallery" title="Creative Gallery" icon={Images}>
        <Tabs defaultValue={PROFILE.gallery.tabs
